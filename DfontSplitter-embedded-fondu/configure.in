dnl Process this file with autoconf to produce a configure script.

AC_INIT(fondu.c)
AC_PROG_CC
AC_CANONICAL_HOST
export HOST=$host

AC_SUBST(VPATH)
AC_SUBST(HOST)

AC_OUTPUT(Makefile)
chmod -w Makefile
